name: waveshare
description: "WaveShare - Offline File Sharing App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase (Stable versions for Flutter 3.9–3.24)
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  firebase_messaging: ^16.0.4

  # State Management
  flutter_riverpod: ^3.0.3

  # Networking
  http: ^1.1.0

  # Local Storage
  shared_preferences: ^2.2.2

  # File Handling
  file_picker: ^10.3.3
  image_picker: ^1.0.4

  # Permissions
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
